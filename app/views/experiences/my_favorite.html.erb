<div class="container text-center">
  <div class="row align-items-center justify-content-between mb-4 mt-5">
    <div class="col-12 col-md-6">
        <h2 class="text-start mb-0"> Favorited dates</h2>
    </div>
  </div>

  <div class="row justify-content-center">
    <% if @experiences.length.positive? %>
      <% @experiences.each do |experience| %>
        <div class="col">
            <%= render 'experiences/index_card', {experience: experience} %>
        </div>
      <% end %>
    <% else %>
      <h4> You have No current Favorites 💔💔 </h4> 
      <h4><%= link_to "Lets find some", experiences_path %> </h4> 
    <% end %>
  </div>
</div>
